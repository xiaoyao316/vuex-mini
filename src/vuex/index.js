import { Store, install } from './store'
import { mapState, mapGetters, mapActions, mapMutations } from './helpers'

export default {
  Store,
  install,
  mapState,
  mapGetters,
  mapActions,
  mapMutations
}

export {
  Store,
  install,
  mapState,
  mapGetters,
  mapActions,
  mapMutations
}
